#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_cypress@4.9.0@cypress/bin/cypress" "$@"
  ret=$?
else 
  node  "$basedir/../_cypress@4.9.0@cypress/bin/cypress" "$@"
  ret=$?
fi
exit $ret
